import Base from "../components/Base";
function Services() {
    return (
        <Base title="Services we provide" 
        buttonEnabled={true} buttonLink="/" buttonType="warning">
            <div>This is Services page</div>
        </Base>
    )
}

export default Services;